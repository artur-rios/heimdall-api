# Scope Specific Permissions

Create and implement a new Use Case, in which a scope owner can create, read, update and delete one or more scope specific permissions. The permissions must have an Id, a PublicId, a Name, a Description and a flag indicating with the permission must be added as a claim on the JWT or not.
